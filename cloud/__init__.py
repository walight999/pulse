"""Cloud sync, auth, encryption, and billing — Phase 1 stubs.

Each module exposes the interface that the desktop app will call.
Implementation requires:
  - A Supabase (or equivalent) project
  - A Stripe account
  - The deployment described in ROADMAP.md Phase 1

These stubs raise NotImplementedError so the rest of the app can import
them without crashing — wire up your backend to make them real.
"""
