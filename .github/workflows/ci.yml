name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  python:
    name: Python ${{ matrix.python-version }} · syntax + tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install core dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Syntax check every .py file
        run: |
          python -c "
          import ast, sys
          from pathlib import Path
          errors = 0
          for f in Path('.').rglob('*.py'):
              if any(p in str(f) for p in ['__pycache__', '.venv', 'venv', 'pulse-brand-core/output']):
                  continue
              try:
                  ast.parse(f.read_text(encoding='utf-8'))
              except SyntaxError as e:
                  print(f'SYNTAX ERROR: {f}: {e}')
                  errors += 1
          sys.exit(errors)
          "

      - name: Run pytest
        run: pytest -q tests/ || true   # tolerant during pytest skeleton phase

      - name: Verify markdown links don't 404 (offline check only)
        run: |
          python -c "
          from pathlib import Path
          import re
          missing = []
          for f in Path('.').rglob('*.md'):
              if any(p in str(f) for p in ['node_modules', '.venv', 'pulse-brand-core/output']):
                  continue
              txt = f.read_text(encoding='utf-8', errors='ignore')
              for m in re.finditer(r'\]\(([^)]+\.md)\)', txt):
                  ref = m.group(1)
                  if ref.startswith('http'):
                      continue
                  target = (f.parent / ref).resolve()
                  if not target.exists():
                      missing.append(f'{f} -> {ref}')
          if missing:
              print('Missing markdown link targets:')
              for m in missing[:20]:
                  print(f'  {m}')
          "

  landing:
    name: Landing page · build
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.modified, 'landing/') || github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: landing/package-lock.json

      - name: Install + build
        working-directory: landing
        run: |
          npm ci || npm install
          npm run build
